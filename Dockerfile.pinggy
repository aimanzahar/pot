# Minimal SSH client used by docker-compose to open a Pinggy tunnel.
# We bake ssh into the image so `docker compose up` doesn't reinstall it
# on every container start.
FROM alpine:3.20
RUN apk add --no-cache openssh-client
ENTRYPOINT ["ssh"]
